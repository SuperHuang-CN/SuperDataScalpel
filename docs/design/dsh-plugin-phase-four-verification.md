# 第四阶段第一批验证记录

日期：2026-09-12。范围：全局助手抽屉、会话管理和对话，不包含业务页面上下文、业务结果跳转或永久删除。

## 版本与环境

- DSH `0.1.5-rc.1`，工程插件 `@datascalpel/dsh-plugin@0.4.0`。
- 最终 DSH 镜像：`sha256:a1d448479bde21808e6a36afc83ff885470d20a39110b3a4e058bed44d20b198`。
- DataScalpel：`http://localhost:18887`，Admin：`http://127.0.0.1:18080`。
- DSH 原生页面：`http://127.0.0.1:13080`；阶段二验证台：`http://127.0.0.1:13081`。
- 所有 DataScalpel 启动和重启均使用根 `./start-local-dev.sh`，没有另起应用或数据库。DSH 原数据卷、配置和原生入口保留。

## 自动化结果

| 验证 | 结果 |
| --- | --- |
| 插件测试 | 25 项通过，包含原有 21 项及元数据迁移、归档执行边界、持久化失败、并发到达消息下的游标分页 |
| 前端针对性测试 | 14 项通过，包含 SSE 分片／UTF-8／认证头／释放、消息去重、临时回答、工具失败状态Markdown 内容边界及共享 HTTP 契约 |
| 后端针对性测试 | `DshSessionManagementTest` 3 项通过：标题校验、查询编码、游标与旧分页模式 |
| 编译与构建 | Admin 及依赖编译通过；UI 生产构建通过；插件构建和镜像构建通过 |
| 新增前端模块静态检查 | 通过 |

后端使用工程 Wrapper/settings 解析依赖，只编译并执行本阶段测试，未扩大到全工程测试编译。共享 HTTP 测试中原有校验错误文案断言已与现行“附带字段问题”语义对齐，未修改原 JSON 错误行为。

构建保留工程已有大包／动态导入提示。本次启动期间，其他正在开发的方言代码一度缺少导入；该代码随后由原开发工作补齐，本任务未修改方言实现。启动脚本另检测到 Runner 过期，按脚本要求通过 Wrapper 重建后恢复运行。

## 真实 Admin／DSH 验证

维护脚本：`integrations/dsh-plugin/scripts/verify-phase-four.py`。使用第三阶段 Alice、Bob 两个真实测试账号，仅创建第四阶段隔离会话；不新增、修改或删除业务数据，不改变 MCP 开放清单。

已通过：

- 同一客户端 UUID 重复创建只产生一个会话；标题写入、空标题校验和查询参数检查。
- 两用户不能更新、归档或恢复对方会话；跨用户 404。
- 两个真实模型并发发起原生追问，分别接受选项和自由文本；重复答案失效。
- 追问期间归档被拒绝，SSE 返回回答增量；游标分批读取与原分页完整历史一致。
- 断开观察者不会取消模型任务，重新订阅得到快照；两个用户分别完成受当前权限约束的只读 MCP 对话。
- 独立取消、归档后 Agent 释放、已归档会话拒绝发送和恢复 Agent；归档列表筛选正确，历史仍可读取。
- 恢复归档不会自动加载 Agent；更新展示属性不改变最近活动时间。
- DSH 重启及 Admin 独立重启后，两个用户标题和归档状态仍在；重新登录后均恢复并准确回答此前记住的标记。

控制脚本首次关闭 Python SSE 读取器时，读线程产生 `AttributeError`，不影响业务断线语义及断言；验证工具已补充关闭期间异常处理。

脚本状态在不提交的 `.local/dsh-phase-four/verify-state.json`，凭据继续使用原受保护的第三阶段状态文件。报告及脚本输出不包含令牌或模型密钥。

## 浏览器验证

在现有开发页面实际验证顶部入口、自动初始化、创建会话、SSE 已连接、真实追问及回答、Markdown 表格／代码块、重命名，以及关闭重开保留草稿。

助手开启时成功导航到数据源页面，当前会话及待回答问题保留，背景页面仍可操作。当前截图视口为 1280×720，默认 560px 抽屉内代码横向滚动被限制在代码块内，输入区固定可见。

另已实际验证归档确认、普通列表移除、已归档列表保留标题、归档历史只读、恢复后重新出现输入区，以及 Admin 重启后重新登录能看到原会话。

宽窄布局调整后在 1864px 视口复测：窄模式实测 745.6px（40%）；宽模式实测 1491.2px（80%），左侧会话 Panel 为 280px，右侧对话区为 1211.2px。宽模式选择会话时左侧保持常驻；切回窄模式后会话 Panel 以 360px 覆盖层打开，当前对话和输入区仍保留。关闭并重新打开助手后宽窄偏好恢复。700×800 视口下抽屉实测占满 700px，并强制采用窄模式布局、隐藏宽窄切换入口。页面控制台没有发现本次布局引入的运行错误；现有 Ant Design 兼容性弃用提示不属于本次变更。

## 明确边界

- 会话归档保留原生历史与个人文件，不能代替永久删除。
- 只复用第三阶段允许的工具集合，没有新增任意 Shell、Skills 或文件传输。
- 未接入业务页面上下文和业务结果跳转，本批完成不代表整个第四阶段完成。
