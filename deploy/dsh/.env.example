# Copy to .env.bridge (ignored by Git). Generate an independent random secret.
DSH_BRIDGE_ENABLED=false
DSH_BRIDGE_PROVIDER=deepseek-official
DSH_BRIDGE_MODEL=deepseek-flash
DATASCALPEL_DSH_BRIDGE_TOKEN=
