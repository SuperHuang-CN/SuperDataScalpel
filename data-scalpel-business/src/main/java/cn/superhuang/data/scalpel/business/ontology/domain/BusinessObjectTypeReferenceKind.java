package cn.superhuang.data.scalpel.business.ontology.domain;

/** Current object-type definition resources that must not be silently removed. */
public enum BusinessObjectTypeReferenceKind {
    MODEL,
    MODEL_FIELD,
    OBJECT_TYPE
}
