package cn.superhuang.data.scalpel.dialect.model;

/** How strongly the physical schema identifies the declared spatial definition. */
public enum SpatialMetadataStrength {
    NONE,
    DECLARED,
    ENFORCED
}
