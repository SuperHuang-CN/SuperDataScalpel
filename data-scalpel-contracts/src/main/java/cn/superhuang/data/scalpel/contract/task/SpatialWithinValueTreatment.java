package cn.superhuang.data.scalpel.contract.task;

/** Explicit quantity treatment; missing values preserve legacy raw-value statistics. */
public enum SpatialWithinValueTreatment {
    ORIGINAL_VALUE,
    APPORTION_TOTAL
}
