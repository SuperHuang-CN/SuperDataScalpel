/**
 * Stable, database-neutral contracts shared by Admin and Task Engine during Canvas compilation.
 *
 * <p>This package owns the compilation request, response, Canvas node definitions and metadata
 * snapshot types. Neither side may redefine these wire structures locally.</p>
 */
package cn.superhuang.data.scalpel.contract.task;
