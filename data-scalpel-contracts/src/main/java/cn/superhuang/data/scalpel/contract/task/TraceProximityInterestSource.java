package cn.superhuang.data.scalpel.contract.task;

/** How Trace Proximity Events receives the entities that start the trace. */
public enum TraceProximityInterestSource {
    ENTITY_IDS,
    TABLE
}
