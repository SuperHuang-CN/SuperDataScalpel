package cn.superhuang.data.scalpel.shapefile.model;

/** DBF field categories supported by the phase-one reader. */
public enum ShapefileFieldType {
    STRING,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    DATE
}
