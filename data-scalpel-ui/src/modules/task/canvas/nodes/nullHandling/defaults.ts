import type { NullHandlingConfiguration } from "../../canvasTypes";

export const createNullHandlingConfiguration = (): NullHandlingConfiguration => ({ operations: [] } as unknown as NullHandlingConfiguration);
